---
name: flowchart
description: Process flow with boxes and arrows
family: diagram
needs_interactivity: false
keywords:
  - flowchart
  - flow
  - diagram
  - process flow
  - pipeline
---

Inline SVG. Boxes (rect) + arrows (path with marker-end). ≤ 6 nodes. Each node `<rect>` is clickable — add `data-bap-prompt="Explain: <node label>"` and `style="cursor:pointer"` on every box.

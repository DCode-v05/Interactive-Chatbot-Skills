---
name: table
description: Tabular comparison or feature matrix
family: static
needs_interactivity: false
keywords:
  - table
  - matrix
  - compare
  - feature
  - spec
  - rows
  - columns
---

Compact grid. ≤ 4 columns. Highlight winners per row. Each `<tr>` in `<tbody>` is clickable — add `data-bap-prompt="Tell me more about: <row name>"` and `cursor:pointer` on the row so the user can drill into any row.
